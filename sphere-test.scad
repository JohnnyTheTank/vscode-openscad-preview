// Sphere rendering test
$fn = 32; // Smooth sphere

// Simple sphere
sphere(r=12);

// Note: This should now render as an actual sphere mesh
// instead of the basic cube from the old mock implementation
